on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        crystal: [1.19.1]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Download source
        uses: actions/checkout@v6
      - name: Install Crystal
        uses: crystal-lang/install-crystal@v1
        with:
          crystal: ${{ matrix.crystal }}
      - name: Install libraries
        run: |
          if [ "$RUNNER_OS" == "macOS" ]; then
               brew install libxml2
          elif [ "$RUNNER_OS" == "Linux" ]; then
               # See: https://github.com/crystal-lang/crystal/issues/7177
               sudo apt update && sudo apt install -y lld
          fi
        shell: bash
      - name: Cache shards
        uses: actions/cache@v5
        with:
          path: lib
          key: shards-${{ runner.os }}-${{ hashFiles('shard.lock') }}
          restore-keys: |
            shards-${{ runner.os }}-
      - name: Install shards
        run: shards install
      - name: Run tests
        run: |
          if [ "$RUNNER_OS" == "macOS" ]; then
               export PKG_CONFIG_PATH="$PKG_CONFIG_PATH:/usr/local/opt/libxml2/lib/pkgconfig"
               link_flags=()
          elif [ "$RUNNER_OS" == "Linux" ]; then
               link_flags=(--link-flags=-fuse-ld=lld)
          fi
          find spec -name "*_spec.cr" -print0 | xargs -0 -n 25 crystal spec "${link_flags[@]}"
        env:
          LOG_LEVEL: NONE
      - name: Build
        run: shards build
      - name: Install Node
        uses: actions/setup-node@v6
        with:
          node-version: 22
      - name: Cache node modules
        uses: actions/cache@v5
        with:
          path: node_modules
          key: node-${{ runner.os }}-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            node-${{ runner.os }}-
      - name: Install npm dependencies
        run: npm ci
      - name: Audit npm dependencies
        run: npm audit
      - name: Run npm tests
        run: npm run test
